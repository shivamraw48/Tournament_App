## SERVER.JS
1-FIRST DONE THE SETUP AND IMPORTED REQUIRED         LIBRARIES LIKE NODEJS,CORS,MONGODB,NODEMON..
2-now we setup the mongodb atlas and stored the connection string in .env and created a .gitignore which contains .env and node modules..
3- We created model--user.model.js to store the users data and made the models and schemass..
4- then we created routes folder---users.js for apis  of register and login setuped the apis in server.js at point 6
5- then we created tournaments.model.js and tournaments.js to create a schema and api respectivelyy
6- then we moved forward for frontend and created the component tournamentsList.js in frontend/src and tournamentdetail.js for tournment details



